---
sidebar_position: 1
---

import { CategoryDocCardList } from '@site/src/components/CategoryDocCardList';

# はじめに

> バリデーションのための各種関数を提供します

## 使用方法

`Application::getClass`メソッドを通じて`Validation`クラスを読み込むことができます。

```php title="Controller"
$validation = $this->getClass('Validation');

// 必須チェック
$name = 'フィールドラベル';
$id   = 'field_name';
$validation->required($id, $this->in($id), array('@required', $name));

// エラー取得
$errorMessage = $validation->getErrorMessage();
```

## 関数

<CategoryDocCardList label="関数" />
